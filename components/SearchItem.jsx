import React from 'react'
import { View } from 'react-native'

const SearchItem = (word, partOfSpeech, meaning) => {
  return (
      <View>
          <Text> wordItem  </Text>
    </View>
  )
}

export default SearchItem
